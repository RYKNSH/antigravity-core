---
description: Level 1（Supervised）+ /go のショートカット
---

# /l1 - Supervised Mode Shortcut

## 使用方法

```bash
/l1 "タスク"
```

**実行内容**:
1. `/level 1` を実行（Supervised Mode）
2. `/go "タスク"` を実行

**効果**: 設計承認 + 破壊的操作 + 情報不足で確認

---

> [!TIP]
> `/l1 "タスク"` = `/level 1` + `/go "タスク"`
