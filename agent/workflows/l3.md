---
description: Level 3（Full Auto）+ /go のショートカット
---

# /l3 - Full Auto Mode Shortcut

## 使用方法

```bash
/l3 "タスク"
```

**実行内容**:
1. `/level 3` を実行（Full Auto Mode）
2. `/go "タスク"` を実行

**効果**: 本番デプロイのみ確認。最大自律

---

> [!TIP]
> `/l3 "タスク"` = `/level 3` + `/go "タスク"`
