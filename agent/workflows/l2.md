---
description: Level 2（Autonomous）+ /go のショートカット
---

# /l2 - Autonomous Mode Shortcut

## 使用方法

```bash
/l2 "タスク"
```

**実行内容**:
1. `/level 2` を実行（Autonomous Mode）
2. `/go "タスク"` を実行

**効果**: 破壊的操作 + 情報不足のみ確認（デフォルト）

---

> [!TIP]
> `/l2 "タスク"` = `/level 2` + `/go "タスク"`
