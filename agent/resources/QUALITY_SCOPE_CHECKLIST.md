# Quality Scope Checklist — 世界最高水準 v3

> `/debug-deep`, `/verify`, `/error-sweep`, `/ship` で使用。
> 6 Tier × 30 Layer × 130+ チェックポイント。
> 「エラーなし」を宣言するには、対象Tierの **全レイヤーをPASS** する必要がある。

---

## 🔴 Tier 1: MUST（ビルド・型・ランタイム）
> これが通らないと何も始まらない

### L01: ビルド整合性
- [ ] `npm run build` / `tsc --noEmit` エラー0（全パッケージ）
- [ ] lint 0 warnings（ESLint/Biome）
- [ ] 未使用 import・変数・型の除去
- [ ] strictモード有効（`strict: true` in tsconfig）

### L02: ランタイム安全性
- [ ] 空catch文 0件
- [ ] 全API routeにtry-catch + エラーレスポンス
- [ ] 全fetchに失敗時ハンドリング
- [ ] 環境変数 `!` assertion の妥当性確認
- [ ] null/undefined アクセス保護（`?.`/`??`）
- [ ] Promise の unhandled rejection がない

### L03: ルーティング・パス整合
- [ ] 全fetch URLに定数使用（`API_BASE` 等）
- [ ] ハードコードURL 0件
- [ ] basePath と全リンクの整合性
- [ ] middleware の allow-list に漏れがない

---

## 🟡 Tier 2: SHOULD（セキュリティ・データ）
> 本番に出すなら必須

### L04: 認証・認可
- [ ] 全保護ルートにセッションチェック
- [ ] クライアント側のロールガード（admin等）
- [ ] APIキー・シークレットがコードにない
- [ ] CSRF対策（SameSite cookie / token）
- [ ] Clickjacking防止（X-Frame-Options / CSP）

### L05: データ保護
- [ ] RLSポリシーが適切（anon keyで見えるべきでないデータ）
- [ ] 入力サニタイズ（SQLi, XSS, Prompt Injection）
- [ ] PII がログに漏れていない
- [ ] CORS の適切な設定
- [ ] レスポンスにsensitive data が漏れていない（password hash等）

### L06: データ整合性
- [ ] DBカラム名がコードと一致
- [ ] JOIN条件の正確性（FK関係）
- [ ] マイグレーションとコードのスキーマ一致
- [ ] 命名一貫性（`user_id` vs `discord_user_id`）
- [ ] インデックスが必要なクエリに張られている

### L07: コード衛生
- [ ] `// TODO`, `// HACK`, `// FIXME` が0件
- [ ] `console.log` が本番に漏れていない
- [ ] ハードコードのマジックナンバーが定数化
- [ ] デッドコード（到達不能分岐）の除去
- [ ] 重複ロジックの統合

---

## 🟢 Tier 3: BETTER（UX・アクセシビリティ）
> ユーザーが「気持ちいい」と感じるか

### L08: UX品質
- [ ] 全ページにloading state
- [ ] エラー表示がアプリテーマと一致
- [ ] フォームバリデーション + フィードバック
- [ ] 空状態（データ0件）の表示
- [ ] 成功フィードバック（toast / confetti / アニメーション）
- [ ] 破壊的操作に確認ダイアログ

### L09: アクセシビリティ（WCAG 2.1 AA）
- [ ] `<img>` に意味のある `alt`
- [ ] 装飾要素に `aria-hidden="true"`
- [ ] インタラクティブ要素に `aria-label`
- [ ] キーボードナビゲーション可能
- [ ] カラーコントラスト比 4.5:1 以上
- [ ] フォーカスインジケータ可視
- [ ] スクリーンリーダーで意味が通るか

### L10: レスポンシブ・クロスブラウザ
- [ ] モバイル（375px）表示崩れなし
- [ ] タブレット（768px）表示崩れなし
- [ ] Safari / Chrome / Firefox 互換
- [ ] タッチターゲット 44px以上
- [ ] Safe area対応（notch / ホームバー）

---

## 🔵 Tier 4: ROBUST（耐障害性・エッジケース）
> 「壊れた時にどう振る舞うか」

### L11: エラー回復・レジリエンス
- [ ] API失敗時のリトライ（指数バックオフ + ジッター）
- [ ] フォールバック値で安全に動作
- [ ] fetch に AbortController タイムアウト
- [ ] サーキットブレーカー（連続失敗時の保護）
- [ ] Graceful degradation（部分故障でも全体は動く）
- [ ] エラー種別の分類（一時的 vs 永続的）

### L12: 並行性・競合
- [ ] 同時リクエストでのデータ競合防止
- [ ] useEffect クリーンアップ（return cleanup）
- [ ] イベントリスナーの解除漏れ
- [ ] setInterval/setTimeout のクリア
- [ ] Stale closure問題（useCallback/useRef）
- [ ] 楽観的更新の整合性

### L13: エッジケース・境界値
- [ ] null, undefined, 空配列でクラッシュしない
- [ ] 超長文字列（10,000文字）の処理
- [ ] 特殊文字・絵文字の正常処理
- [ ] 0件, 1件, 10,000件での表示
- [ ] 日本語テキストの折り返し・切れ
- [ ] 負数・ゼロ除算・Infinity

### L14: 冪等性
- [ ] 同じリクエストを2回送っても安全
- [ ] トークン付与の二重実行防止
- [ ] 進捗更新の重複防止（upsert）
- [ ] Webhook 再送対応
- [ ] DBのUNIQUE制約で整合性保証

---

## 🟣 Tier 5: PRODUCTION（運用・インフラ）
> 本番で安定稼働するか

### L15: レート制限・外部API
- [ ] Discord API 429ハンドリング + 待機
- [ ] OpenAI API レート制限 + コスト上限
- [ ] Supabase 接続プール管理
- [ ] リトライストーム防止（同時大量リトライ）

### L16: タイムゾーン・時間処理
- [ ] JST/UTC の一貫性
- [ ] `new Date()` のタイムゾーン安全性
- [ ] 日付比較が正しい（`created_at` 等）
- [ ] cron/スケジュールの時間帯考慮
- [ ] サマータイム非対応地域（日本）の前提明記

### L17: パフォーマンス
- [ ] N+1クエリ排除
- [ ] 不要な再レンダリング防止（deps配列）
- [ ] 画像最適化（WebP, lazy load）
- [ ] バンドルサイズ最適化
- [ ] SSR/CSR の適切な使い分け
- [ ] 重い処理のWeb Worker移行

### L18: 可観測性（Observability）
- [ ] 構造化ログ（`[Handler]` プレフィックス統一）
- [ ] ヘルスチェックエンドポイント
- [ ] エラー通知（Sentry / Discord webhook）
- [ ] キー操作の監査ログ
- [ ] レスポンスタイム計測（P95/P99）

### L19: デプロイ・ロールバック
- [ ] 環境一貫性（dev/staging/prod）
- [ ] Feature Flag（壊れた機能をOFFにできる）
- [ ] マイグレーションのロールバック手順
- [ ] Zero-downtime deploy
- [ ] カナリアリリース（段階的展開）

### L20: 依存関係・サプライチェーン
- [ ] `npm audit` 脆弱性0件
- [ ] 依存パッケージのライセンス互換性
- [ ] lockfile の一貫性（package-lock.json）
- [ ] 依存の鮮度（メジャーバージョン遅れ確認）

---

## ⚫ Tier 6: WORLD-CLASS（カオス・進化）
> Netflix / Google / Stripe が当たり前にやっていること

### L21: カオスエンジニアリング
- [ ] ランダムプロセスキルで回復するか
- [ ] ネットワーク遅延注入（100ms → 5s）でUXどうなるか
- [ ] DB接続切断時の振る舞い
- [ ] 外部API全ダウン時の振る舞い
- [ ] メモリ枯渇シミュレーション

### L22: コントラクトテスト
- [ ] bot ↔ web API のリクエスト/レスポンス契約
- [ ] DB スキーマ変更時の後方互換性チェック
- [ ] 外部API（Discord, OpenAI）の応答変更検知
- [ ] Consumer-Driven Contract（CDC）

### L23: ミューテーションテスト
- [ ] テストが本当にバグを検知できるか（テストの品質テスト）
- [ ] コード変異させてテストが落ちるか確認
- [ ] カバレッジ100%でもバグを見逃すケースの検証

### L24: プロパティベーステスト（Fuzzing）
- [ ] ランダム入力で予期しないクラッシュがないか
- [ ] 文字列・数値・オブジェクトの自動生成テスト
- [ ] 入力空間の組み合わせ爆発への対策

### L25: ビジュアルリグレッション
- [ ] スクリーンショット比較（Chromatic / Percy）
- [ ] コンポーネントの意図しない見た目変化検知
- [ ] ダークモード/ライトモード両方のスナップショット

### L26: SRE（Site Reliability Engineering）
- [ ] SLO定義（99.9% 稼働率 etc）
- [ ] エラーバジェット管理
- [ ] インシデント対応手順（Runbook）
- [ ] Postmortem テンプレート
- [ ] オンコールローテーション

### L27: コスト監視
- [ ] API呼び出しコストの上限設定
- [ ] インフラコストのアラート
- [ ] 不要リソースの自動クリーンアップ
- [ ] コスト/ユーザーのトラッキング

### L28: データライフサイクル
- [ ] データ保持ポリシー（90日/1年 etc）
- [ ] 退会時のデータ完全削除フロー
- [ ] バックアップ＆リストア検証（RTO/RPO）
- [ ] データエクスポート機能（GDPR対応）
- [ ] PII暗号化（at rest / in transit）

### L29: ドキュメント・保守性
- [ ] 複雑なロジックのインラインコメント
- [ ] APIのJSDoc/OpenAPI定義
- [ ] README/環境構築手順が最新
- [ ] アーキテクチャ図（ADR）
- [ ] 認知的複雑度スコア（Cognitive Complexity）
- [ ] バスファクター≥2（1人欠けても保守可能）

### L30: テスト戦略・成熟度
- [ ] テストピラミッド（Unit > Integration > E2E）
- [ ] テストの独立性（並列実行可能、順序無依存）
- [ ] テストデータの自動生成（Factory/Fixture）
- [ ] CI/CD パイプラインでの自動実行
- [ ] Flaky test検知＆自動スキップ
- [ ] テスト実行時間の最適化（並列化）

---

## 適用ガイドライン

| コマンド | 必須 | 推奨 | 理想 |
|---------|------|------|------|
| `/debug-deep` | Tier 1-2 | Tier 3-4 | — |
| `/verify` | Tier 1-3 | Tier 4 | Tier 5 |
| `/error-sweep` | Tier 1-4 | Tier 5 | — |
| `/fbl` | Tier 3 | Tier 4 | — |
| `/ship` | **Tier 1-5** | Tier 6 | — |
| IPO / 年次監査 | **Tier 1-6 全て** | — | — |

## grep / CLI リファレンス

```bash
# — Tier 1 —
rg 'catch\s*\{\s*\}' --glob '*.ts' --glob '*.tsx' -l          # 空catch
npx tsc --noEmit --noUnusedLocals                              # 未使用変数

# — Tier 2 —
rg '// (TODO|FIXME|HACK)' --glob '*.ts' --glob '*.tsx' -l     # TODO残骸
rg 'console\.log\(' --glob '*.ts' --glob '*.tsx' -l src/       # log漏れ
rg "'/academy/" --glob '*.ts' --glob '*.tsx'                   # ハードコードURL

# — Tier 3 —
rg 'alt=""' --glob '*.tsx'                                     # 空alt
npx lighthouse http://localhost:3000 --output=json             # Lighthouse

# — Tier 4 —
rg 'setInterval|setTimeout' --glob '*.tsx' -l                  # cleanup漏れ
rg '\.push\(|\.splice\(' --glob '*.tsx' -l                     # 直接mutation

# — Tier 5 —
npm audit --production                                          # 脆弱性
npx depcheck                                                    # 未使用依存
npx bundlesize                                                  # バンドルサイズ

# — Tier 6 —
npx stryker run                                                 # ミューテーション
npx fast-check                                                  # プロパティテスト
```
