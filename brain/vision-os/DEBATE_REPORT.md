# 🏁 Debate Report — VISION.md 精錬
> Preset: Titan (Jensen + Steve + Elon + Skeptic)
> Mode: /debate deep (最低3ラウンド)

## 👥 Debate Team
- **Jensen** 🧥 (Moderator): Alignment管理
- **Steve** 🍏: 品質への執着、シンプリシティ
- **Elon** 🚀: First Principles、物理的制約
- **Skeptic** 🤔: 根本を問う

---

## 🔄 Round 1: Initial Critique

**🍏 Steve**: VISION.mdの「Feeling」セクションは美しい。だがアーキテクチャ図が**醜い**。ASCIIアートで3つのLayerを描いているが、読む人間がそれで「Wow」と言うか？本質的な問題は**Layer 2 Quality Loopのリトライ上限が安易すぎる**こと。「5回で諦めて報告」？ **それは妥協だ。** ゼロエラーを約束したなら、5回で解決しない問題は**問題の分解が足りない**ということ。リトライではなく**分解**すべき。

**🚀 Elon**: Heartbeat Daemonの設計に物理的な問題がある。**SSDにデーモンは常駐できない。** SSDは外部ストレージだ。macOSのlaunchdやcronで常駐プロセスを動かすにはホストOS側の設定が必要で、それは「SSD差したら動く」と矛盾する。**First Principles: SSDは実行環境ではなく、設定と状態の保持装置だ。** 実行はホストOS上で行い、SSDはその設計図を持つだけ。起動スクリプトをSSD上に置き、接続時に自動実行させるアプローチが正しい。

**🤔 Skeptic**: 根本的な疑問。**「完全自律」は本当に可能か？** 現実を見よう — AIコーディングアシスタントは1つの会話コンテキスト内でしか動けない。会話が切れたらAIは消える。それなのに「人間がいなくても動くSSD」とは矛盾しないか？**Session Continuityの`session_state.json`は「誰が」読むのか？** 人間が次のセッションを開始しないと、何も起きないのでは？

**🧥 Jensen (Moderator)**: 3つの重大な論点が出た。整理する:
1. **品質ループの設計**: リトライ上限 vs 問題分解（Steve）
2. **物理的制約**: SSDにデーモンは常駐できない（Elon）
3. **自律性の限界**: AI会話コンテキストの制約（Skeptic）

→ **Round 2でこれらを深掘りする。**

---

## 🔄 Round 2: Deep Dive

**🚀 Elon (物理的解決策)**: デーモン問題を解決する。

```
アプローチ: SSD接続時の自動起動（macOS LaunchAgent）
1. SSD上に setup.sh を配置
2. setup.sh が LaunchAgent plist を ~/Library/LaunchAgents/ にコピー
3. LaunchAgent が SSD 接続を検知し、heartbeat.js を起動
4. heartbeat.js が SSD 上の queue/ を監視し、タスクを実行
5. SSD 切断時 → heartbeat.js が graceful shutdown
```

**ポイント**: SSDは**設計図と状態の保存場所**。実行はホストOS。だがユーザー体験としては「SSD差したら動く」を実現できる。コストは**初回セットアップのみ**（`/project-init` で自動化）。

**🍏 Steve (品質ループ再設計)**: リトライ上限は捨てる。代わりに:

```
Quality Loop v2: 問題分解アプローチ
1. 実装 → 検証
2. 失敗 → エラーを分類:
   a. Trivial (lint, typo) → 即自動修正、再検証
   b. Logic (テスト失敗) → 原因分析 → ピンポイント修正 → 再検証
   c. Design (設計レベル) → タスクを分解して queue に戻す
3. c の場合のみ「ここで設計判断が必要」と報告
```

**つまり、「5回リトライして諦める」ではなく、「解けない問題は小さく分解する」。** これなら「明らかなエラーが残ったまま報告される」は起きない。報告されるのは「設計判断が必要な問題」だけ。

**🤔 Skeptic (自律性の再定義)**: Elon の LaunchAgent アプローチで「SSD接続 → 自動起動」は解決する。だが**AIのコンテキスト問題**が残る。headless（ブラウザなし）環境でAIを呼ぶには**LLM APIを直接叩く**しかない。つまり:

```
heartbeat.js
  ├── タスク読み込み（SSD queue/から）
  ├── LLM API 呼び出し（OpenAI / Anthropic / Gemini）
  ├── レスポンスをファイルに書き出し
  ├── コマンド実行（npm run build, ruff check等）
  ├── 結果をLLMに再投入（フィードバックループ）
  └── 完了 → completed/ に移動
```

これならAIのコンテキスト問題を回避できる。**ただしコスト面のリスク**がある — 無限ループしたら API 請求が爆発する。**コストガードレール**（1タスクあたりの最大API呼び出し回数 or 金額上限）が必須。

**🧥 Jensen (Moderator)**: Round 2で解決策が見えてきた:
1. ✅ 品質ループ → 問題分解アプローチ（Steve）
2. ✅ 物理的制約 → LaunchAgent + SSD接続検知（Elon）
3. ✅ 自律性 → LLM API直接呼び出し + コストガード（Skeptic）

→ **Round 3: あえてアラ探しをする（deep mode義務）。**

---

## 🔄 Round 3: Stress Testing

**🤔 Skeptic**: **セキュリティの穴。** LLM APIキーがSSD上の`.env`に平文で保存されている。SSDを紛失したら全APIキーが漏洩する。自律実行中にAPIキーを使ってコード生成 → コミット → デプロイまで自動なら、**悪意あるプロンプトインジェクション**のリスクもある。`queue/tasks/`に悪意あるタスクファイルを配置されたら？

**🚀 Elon**: セキュリティはシンプルに解決する:
1. **APIキー**: macOS Keychainに保存。SSD上には参照キー名のみ → SSD紛失時のリスクゼロ
2. **タスクファイル**: ハッシュ検証（`tasks/*.md`に対するSHA256チェックサム）→ 改ざん検知
3. **実行権限**: Approval Gateの3カテゴリ（OS/Payment/Auth）はすべてKeychain経由のAPIキーが必要 → 物理的に自動実行不可能

**コスト削減**: 上記3つで十分。それ以上のセキュリティは過剰。"The best part is no part." — 要らないセキュリティ層は削除。

**🍏 Steve**: **UXの問題。** 「完了通知」はどう届く？ push通知と言ったが、**何で通知する？** Discord webhook？macOS通知？LINE？ 通知チャネルが決まっていないのは致命的。ユーザーが気づかなければ、自律で完成しても意味がない。**通知はUIの一部。UI決定は最初に行うべき。**

**🚀 Elon**: 通知は**Discord webhook**一択で始める。理由:
1. 既にDiscord連携スクリプトがSSD上に存在（`discord_poster.js`）
2. スマホ通知がデフォルトで有効
3. リッチフォーマット（embed）でタスク完了詳細を送れる
4. 承認アクション → Discordのボタンコンポーネントで「Approve / Reject」
将来的にLINE等を追加する場合も、webhook抽象化層を1つ噛ませれば済む。

**🧥 Jensen (Moderator)**: Round 3の成果:
1. ✅ セキュリティ → macOS Keychain + ハッシュ検証（Elon）
2. ✅ 通知チャネル → Discord webhook + ボタン承認（Elon + Steve）
3. ✅ コストガード → 1タスクあたりのAPI呼び出し上限（Skeptic）

**全員の懸念が出尽くした。Synthesis に進む。**

---

## 💎 Refined Proposal (The Output)

### VISION.md v2 への変更点

| 項目 | v1 | v2 (議論後) |
|------|-----|-------------|
| **Heartbeat** | SSD常駐デーモン | macOS LaunchAgent（SSD接続検知→自動起動） |
| **Quality Loop** | リトライ上限5回 | 問題分解アプローチ（Trivial/Logic/Design分類） |
| **自律実行** | 曖昧 | LLM API直接呼び出し（headless） |
| **セキュリティ** | .env平文 | macOS Keychain + SHA256チェックサム |
| **通知** | 未定義 | Discord webhook + ボタン承認 |
| **コストガード** | なし | 1タスクあたりのAPI呼び出し上限 |

### 🛡️ Addressed Concerns
- [解決済み] SSDにデーモン常駐不可 → LaunchAgent方式 (by Elon)
- [解決済み] リトライ上限の安易さ → 問題分解 (by Steve)
- [解決済み] AIコンテキスト制約 → LLM API直接呼び出し (by Skeptic)
- [解決済み] セキュリティ → Keychain + ハッシュ検証 (by Elon)
- [解決済み] 通知チャネル → Discord webhook (by Steve + Elon)

### ⚠️ Remaining Risks (Minor)
- LLM APIの品質が低い場合、自律修正ループが非効率になるリスク（監視で対応）
- macOS以外のOS対応は将来課題（現時点ではmacOS専用で問題なし）

### 📊 Persona Contribution
| Persona | Impact | Key Contribution |
|---------|--------|-----------------|
| Elon 🚀 | **High** | LaunchAgent設計、Keychain、Discord通知、コスト削減 |
| Steve 🍏 | **High** | Quality Loop v2（問題分解）、UX要件定義 |
| Skeptic 🤔 | **High** | セキュリティ穴、AIコンテキスト制約、コストガード |
| Jensen 🧥 | Moderate | 論点整理、ラウンド管理 |

---

> 🧥 **Jensen**: "Alignment achieved. Ship it."
