# VISION.md — The Immortal SSD

> 🍏 Drafted by Steve — "Simplicity is the ultimate sophistication."

---

## The Feeling

SSDを差したら、もう何もしなくていい。

コーヒーを入れて戻ってきたら、昨日のバグは直っていて、新機能のテストは全部通っていて、UIは美しく磨かれている。通知は1つだけ —「Stripeの本番キー更新を承認しますか？」

それだけ。

**SSDは「働く場所」ではない。「完成品が生まれる場所」だ。**

---

## Architecture: The Three Layers

### Layer 1: The Heartbeat（心臓）
SSDに常駐する**プロセスマネージャー**。人間がいなくても脈を打ち続ける。

```
┌─────────────────────────────────────────────┐
│  HEARTBEAT DAEMON                           │
│  ─────────────────                          │
│  PID: /Volumes/PortableSSD/.antigravity/    │
│       heartbeat.pid                         │
│                                             │
│  状態: heartbeat.state.json                 │
│  ┌─────────────────────────────────────┐    │
│  │ current_task: "videdit/fix-export"  │    │
│  │ phase: "verify"                     │    │
│  │ retry_count: 2                      │    │
│  │ last_error: null                    │    │
│  │ quality_gate: "pending"             │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  ループ:                                    │
│  1. タスクキューからpick                     │
│  2. 実行                                    │
│  3. 検証（ゼロエラーまで再試行）             │
│  4. 承認ゲートチェック                       │
│  5. 完了 or 再ループ                         │
└─────────────────────────────────────────────┘
```

### Layer 2: The Quality Loop（品質の輪）
**「明らかなエラーが残ったまま報告される」を根絶する。** これが最も重要。

```
実装 ──→ Lint ──→ Type Check ──→ Build ──→ Test
  ↑                                         │
  │         ❌ 1つでも失敗                    │
  └─────────────────────────────────────────┘
                    │
                    ↓ ✅ 全通過
              Browser Review
              (スクリーンショット + 自己評価)
                    │
                    ↓ ✅ 品質基準クリア
              REPORT TO HUMAN
```

**ルール**: このループを最大N回（設定可能、デフォルト5）回す。N回で解決しなければ「ここで詰まった」と正直に報告。だが**1回目で報告することは絶対にない。**

### Layer 3: The Approval Gate（承認の門）
人間が関与する唯一のポイント。3つだけ:

| カテゴリ | 例 | 通知方法 |
|---------|-----|---------|
| **OS System** | `brew install`, `sudo`, システム設定変更 | Push通知 |
| **Payment** | Stripe API, 課金ロジック変更 | Push通知 |
| **Account/Auth** | OAuth設定, API Key rotation, 権限変更 | Push通知 |

**それ以外は全て自律実行。コミットもデプロイも。**

---

## The Task Queue（タスクキュー）

人間がタスクを投げる方法は3つ:

1. **会話**: `/go "〇〇を作って"` — AIが解釈してキューに追加
2. **ファイル**: `SSD/.antigravity/queue/tasks/*.md` に直接配置
3. **自動検出**: `NEXT_SESSION.md` から前回の残タスクを自動ロード

```
queue/
├── tasks/
│   ├── 001_fix-export-bug.md      # 手動追加
│   ├── 002_add-telop-animation.md # 手動追加
│   └── auto_next_session.md       # 自動生成
├── completed/                      # 完了タスク（ログ）
└── blocked/                        # 承認待ち
```

---

## Session Continuity（セッション持続性）

**セッション間の文脈喪失を完全に解決する。**

```json
// .antigravity/brain/session_state.json
{
  "last_session": "2026-02-10T18:30:00+09:00",
  "active_project": "Videdit",
  "open_tasks": ["fix-export-bug", "telop-animation"],
  "context_snapshot": {
    "recent_files": ["TelopDesigner.tsx", "api.py"],
    "recent_errors": [],
    "git_branch": "feature/telop-v2",
    "git_dirty_files": 3
  },
  "quality_status": {
    "lint": "pass",
    "build": "pass", 
    "test": "2/15 failing"
  }
}
```

次のセッション開始時にこのJSONを読み込み、**「前回の続き」が伝わる。**

---

## One More Thing 🍏

**Self-Healing（自己修復）。**

SSD上のスクリプトが壊れたら？ナレッジが古くなったら？

Heartbeatデーモンは定期的に自分自身の健全性もチェックする:
- ワークフローの依存スクリプトが全て存在するか
- `.env` のAPIキーが有効か（ドライランでテスト）
- ナレッジベースの最終更新日が30日以上前でないか

壊れていたら**自分で直す。** 直せなければ**それだけ**を報告する。

---

> 🍏 **Verdict: This is not a development environment. This is a creative partner that never sleeps.**
