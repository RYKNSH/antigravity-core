# Integrated Workflow Architecture: The 1.5-Command System

## 1. 概要 (Overview)
Antigravity 環境では、AI との対話における認知負荷を本質的な課題解決へ集中させるため、セッション全体を制御する `/go` コマンドと、自然言語による終了トリガーを組み合わせた「1.5コマンド体制」を構築しました。

## 2. コア・コンポーネント (Core Components)

### 2.1 The Master Entry: `/go`
セッションのライフサイクル全体（Setup -> Execute -> Verify）をカプセル化した最上位メタワークフローです。

- **自動セットアップ**: `/checkin` (同期・清掃) と `/dev` (サーバー起動) をバックグラウンドで実行。
- **タスク投入**: 引数としてタスクを渡すか、セッション開始後の通常会話をトリガーとして作業フェーズを開始。
- **カスケード実行**: 実装・検証・修正のループをユーザーの明示的なコマンドなしで自律的に連鎖。

### 2.2 The Execution Engine: "The Big Three"
`/go` の内部で「フェーズ」として呼び出される 3つの目的別メタワークフロー。

- **Layer 1: `/work` (実装)**: `/spec`, `/new-feature`, `/bug-fix`, `/refactor` を内包。
- **Layer 2: `/verify` (検証)**: `/test`, `/fbl`, `/debate team` を統合。120% 品質のゲートキーパー。
- **Layer 3: `/ship` (配信)**: `/build`, `/deploy`, `/db-migrate` を安全に統合。

### 2.3 The Natural Language Terminal: Implicit Endings
明示的なコマンドではなく、自然言語（例：「終わり」「また明日」）を作業終了の意図として検知します。セッション終了時に `NEXT_SESSION.md` を生成し、継続性を担保します。

## 3. The Proactive Suggestion Engine

1.5コマンド体制の究極の進化形は、コマンドの実行すら AI が提案する「自律提案型」です。

### 3.1 AUTO_TRIGGERS.md による制御
AI が状況（コンテキスト）を監視し、特定の条件にヒットした際に「アクション」を提案します。

- **Complexity Trigger**: コードの変更量や複雑度が増した際、`/debate` や `/refactor` を提案。
- **Quality Trigger**: テストの欠如やカバレッジ不足を検知し、`/verify` を提案。
- **Time Trigger**: 一定時間の経過や深夜の作業に対し、コミット勧奨やセッション終了を提案。
- **Inactivity Trigger**: 長時間の沈黙に対し、現状の要約や次のステップの確認を提案。

### 3.2 認知負荷の逆転
ユーザーが「次に何をすべきか（どのコマンドを打つべきか）」を考える負荷を、AI が肩代わりします。ユーザーは AI の提案を「承認」または「却下」するだけでプロセスが進みます。

## 4. 自律連鎖（Chaining）の流れ

1. **User**: `/go "ログインのバグを直して"`
2. **AI**: `[🟢 開始中]` (環境準備) -> `/work` 起動。
3. **AI**: 実装中、コードが複雑化 -> **[Proactive Trigger]**: 「一度リファクタリングして整理しませんか？」
4. **AI**: 実装完了 -> **[Proactive Trigger]**: 「テストがまだですね。検証フェーズに入りますか？」 -> `/verify` 起動。
5. **AI**: セッション長時間化 -> **[Proactive Trigger]**: 「3時間経過しました。進捗をコミットして休憩しませんか？」
6. **User**: 「ありがとう、終わりにするわ」
7. **AI**: `[🟡 終了中]` -> 翌日のための `NEXT_SESSION.md` を書き残してクローズ。

---
*Created: 2026-02-06. Antigravity Global Environment Optimization.*
